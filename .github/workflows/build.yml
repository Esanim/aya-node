name: Build

on:
  push:
    branches:
      - 'main'
      - 'aya-test'

jobs:
  docker-build-push:
    name: Docker build and push
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # Build the Docker image first
      - uses: docker/build-push-action@v4
        with:
          push: false
          file: ./Dockerfile.polkadot
          tags: polkadot-cli:latest

      # Slim it!
      - uses: kitabisa/docker-slim-action@v1
        env:
          DSLIM_HTTP_PROBE: false
        with:
          target: polkadot-cli:latest
          tag: 'slim'

      # Docker Hub Login
      - uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Push to the registry
      - run: |
          docker tag polkadot-cli:slim ${{ secrets.DOCKERHUB_USERNAME }}/polkadot-cli:slim
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/polkadot-cli:slim
